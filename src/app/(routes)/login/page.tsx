import React from "react"
import styles from './page.module.scss'

const Login: React.FC = () => (
    <section>
      <h1 className={styles.pageTitle}>Logga in</h1>
    </section>
);
  
export default Login;